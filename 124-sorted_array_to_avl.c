#include "binary_trees.h"

/**
 * sorted_array_to_avl - array
 *
 * @array: Array to tree
 * @size: Size of array
 *
 * Return: Pointer
*/
avl_t *sorted_array_to_avl(int *array, size_t size)
{
	(void)array;
	(void)size;

	return (NULL);
}
